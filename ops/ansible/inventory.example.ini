; Copy to inventory.ini and fill in real hosts. Not loaded automatically —
; pass explicitly: ansible-playbook -i inventory.ini site.yml

[boxcar_hosts]
prod-app-01.internal ansible_user=deploy
prod-app-02.internal ansible_user=deploy

[boxcar_hosts:vars]
boxcar_vault_name=prod
boxcar_systemd_service=myapp.service
